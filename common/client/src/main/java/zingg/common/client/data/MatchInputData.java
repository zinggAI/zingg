package zingg.common.client.data;

import zingg.common.client.ZFrame;

public class MatchInputData<D, R, C> extends AGenericData<D, R, C> {

    public MatchInputData(ZFrame<D, R, C> data) {
        super(data);
    }
}
